version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
  build:
    commands:
      - echo "Building application..."
      - ls
      - mkdir -p output
      - cp index.html output/
artifacts:
  files:
    - output/**/*
